spring:
  datasource:
    url: "jdbc:h2:mem:test"
    username: "sa"
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
      generate-ddl: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8787

api:
  root: api/v1